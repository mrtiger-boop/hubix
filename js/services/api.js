const API={client:null,ready:false,init(){const u=window.HUBIX_SUPABASE_URL,k=window.HUBIX_SUPABASE_ANON_KEY;if(!window.supabase||!u||!k||u.includes('REMPLACE_')||k.includes('REMPLACE_'))return false;this.client=window.supabase.createClient(u,k);this.ready=true;return true},profile(p){if(!p)return null;return{id:p.id,email:p.email||'',pseudo:p.pseudo||p.email?.split('@')[0]||'Utilisateur',bio:p.bio||'Membre Hubix.',age:+p.age||18,country:p.country||'France',region:p.region||'',city:p.city||'',lang:p.lang||'Français',gender:p.gender||'Peu importe',interests:p.interests||[],goals:p.goals||[],status:p.status||'En ligne',avatar_url:p.avatar_url||null,banner_url:p.banner_url||null,xp:+p.xp||0,level:+p.level||1,rubies:+p.rubies||2500}},async current(){const{data}=await this.client.auth.getUser();if(!data?.user)return null;return this.getProfile(data.user.id)},async getProfile(id){const{data,error}=await this.client.from('profiles').select('*').eq('id',id).maybeSingle();if(error)throw error;return this.profile(data)},async uploadAvatar(id,file){const ext=(file.name.split('.').pop()||'png').toLowerCase(),path=`${id}/avatar-${Date.now()}.${ext}`;const{error}=await this.client.storage.from('avatars').upload(path,file,{upsert:true});if(error)throw error;return this.client.storage.from('avatars').getPublicUrl(path).data.publicUrl},async signUp({email,password,pseudo,age,country,bio,avatarFile}){const{data,error}=await this.client.auth.signUp({email,password});if(error)throw error;if(!data.user)throw new Error('Confirme ton email puis connecte-toi.');let avatar_url=null;if(avatarFile)avatar_url=await this.uploadAvatar(data.user.id,avatarFile);const row={id:data.user.id,email,pseudo,bio:bio||'Membre Hubix.',age:+age||18,country:country||'France',lang:'Français',gender:'Peu importe',goals:['Discuter'],avatar_url};const ins=await this.client.from('profiles').insert(row).select().maybeSingle();if(ins.error)throw ins.error;await this.setPresence(data.user.id,true,'register');return this.profile(ins.data||row)},async signIn({identifier,password}){let email=identifier.trim().toLowerCase();if(!email.includes('@')){const r=await this.client.from('profiles').select('email').eq('pseudo',identifier.trim()).maybeSingle();if(r.error)throw r.error;if(!r.data?.email)throw new Error('Pseudo introuvable.');email=r.data.email}const{data,error}=await this.client.auth.signInWithPassword({email,password});if(error)throw error;const p=await this.getProfile(data.user.id);await this.setPresence(p.id,true,'login');return p},async signOut(id){if(id)await this.setPresence(id,false,'logout');await this.client.auth.signOut()},async updateProfile(p,file){const up={pseudo:p.pseudo,bio:p.bio,age:+p.age||18,country:p.country||'France',status:p.status||'En ligne',updated_at:new Date().toISOString()};if(file)up.avatar_url=await this.uploadAvatar(p.id,file);const r=await this.client.from('profiles').update(up).eq('id',p.id).select().maybeSingle();if(r.error)throw r.error;return this.profile(r.data)},async setPresence(id,on,page=''){if(!id)return;const now=new Date().toISOString();await this.client.from('presence').upsert({user_id:id,is_online:on,page,last_seen:now,updated_at:now})},async onlineCount(){const{count}=await this.client.from('presence').select('*',{count:'exact',head:true}).eq('is_online',true);return count||0},subPresence(cb){return this.client.channel('presence_global').on('postgres_changes',{event:'*',schema:'public',table:'presence'},cb).subscribe()},async worldList(lang,room){const r=await this.client.from('world_messages').select('*,profiles!sender_id(pseudo,avatar_url)').eq('lang',lang).eq('room',room).is('deleted_at',null).order('created_at',{ascending:true}).limit(150);if(r.error)throw r.error;return r.data||[]},async worldSend(lang,room,uid,body){const r=await this.client.from('world_messages').insert({lang,room,sender_id:uid,body});if(r.error)throw r.error},subWorld(lang,room,cb){return this.client.channel(`world_${lang}_${room}`).on('postgres_changes',{event:'INSERT',schema:'public',table:'world_messages'},p=>{if(p.new.lang===lang&&+p.new.room===+room)cb(p.new)}).subscribe()},ageOk(age,range){if(!range||range==='Peu importe')return true;if(range==='51+')return age>=51;const[a,b]=range.split('-').map(Number);return age>=a&&age<=b},score(me,o,prefs){let s=45;if(prefs.country==='Monde entier'||prefs.country===o.country)s+=15;if(prefs.lang===o.lang)s+=15;if(this.ageOk(o.age,prefs.age))s+=15;if(prefs.gender==='Peu importe'||prefs.gender===o.gender)s+=10;return Math.min(99,s)},async findMatch(me,prefs){let q=this.client.from('profiles').select('*,presence!left(is_online)').neq('id',me.id).limit(100);if(prefs.gender!=='Peu importe')q=q.eq('gender',prefs.gender);if(prefs.country!=='Monde entier')q=q.eq('country',prefs.country);const r=await q;if(r.error)throw r.error;let pool=(r.data||[]).map(x=>{let p=this.profile(x);p.is_online=Array.isArray(x.presence)?x.presence[0]?.is_online:x.presence?.is_online;p.compatibility=this.score(me,p,prefs);return p}).filter(p=>p.is_online!==false).filter(p=>this.ageOk(p.age,prefs.age)).sort((a,b)=>b.compatibility-a.compatibility);if(!pool.length)return null;const m=pool.slice(0,10)[Math.floor(Math.random()*Math.min(10,pool.length))];const match=await this.client.from('matches').insert({user_a:me.id,user_b:m.id,compatibility:m.compatibility}).select().maybeSingle();if(match.error)throw match.error;m.match_id=match.data.id;return m},async privateList(matchId){const r=await this.client.from('private_messages').select('*,profiles!sender_id(pseudo,avatar_url)').eq('match_id',matchId).is('deleted_at',null).order('created_at',{ascending:true}).limit(150);if(r.error)throw r.error;return r.data||[]},async privateSend(matchId,uid,body){const r=await this.client.from('private_messages').insert({match_id:matchId,sender_id:uid,body});if(r.error)throw r.error},subPrivate(matchId,cb){return this.client.channel(`private_${matchId}`).on('postgres_changes',{event:'INSERT',schema:'public',table:'private_messages',filter:`match_id=eq.${matchId}`},cb).subscribe()},async add(table,userId,otherId,col){const r=await this.client.from(table).upsert({user_id:userId,[col]:otherId});if(r.error)throw r.error},async listSocial(table,userId,col){const r=await this.client.from(table).select(`${col}, profiles!${col}(*)`).eq('user_id',userId);if(r.error)return[];return(r.data||[]).map(x=>this.profile(x.profiles)).filter(Boolean)},async notifyList(uid){const r=await this.client.from('notifications').select('*').eq('user_id',uid).order('created_at',{ascending:false}).limit(50);return r.data||[]},subNotify(uid,cb){return this.client.channel(`notify_${uid}`).on('postgres_changes',{event:'INSERT',schema:'public',table:'notifications',filter:`user_id=eq.${uid}`},p=>cb(p.new)).subscribe()}};
