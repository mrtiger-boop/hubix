const Friends={async render(){if(!API.ready||!Auth.user){DOM.html('friendList','<p class="muted">Connecte-toi.</p>');return}const f=await API.listSocial('friends',Auth.user.id,'friend_id'),fav=await API.listSocial('favorites',Auth.user.id,'favorite_id'),b=await API.listSocial('blocks',Auth.user.id,'blocked_id');DOM.html('friendList',this.list(f));DOM.html('favoriteList',this.list(fav));DOM.html('blockedList',this.list(b));DOM.text('statFriends',f.length)},list(a){return a.length?a.map(p=>`<div class="card"><b>${p.pseudo}</b><p>${p.age} ans • ${p.country}</p></div>`).join(''):'<p class="muted">Aucun.</p>'}};
