const Router={init(){document.querySelectorAll('[data-page]').forEach(b=>b.addEventListener('click',()=>this.show(b.dataset.page)));DOM.el('menuToggle')?.addEventListener('click',e=>{e.stopPropagation();DOM.el('menuPanel')?.classList.toggle('open')});document.addEventListener('click',e=>{if(!DOM.el('menuPanel')?.contains(e.target)&&e.target.id!=='menuToggle')DOM.el('menuPanel')?.classList.remove('open')})},show(page){document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));DOM.el(page)?.classList.add('active');document.querySelectorAll('.nav').forEach(b=>b.classList.toggle('active',b.dataset.page===page));DOM.el('menuPanel')?.classList.remove('open');if(window.Presence)Presence.ping(page)}};
